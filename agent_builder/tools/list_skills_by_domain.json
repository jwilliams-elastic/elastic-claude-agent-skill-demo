{
  "id": "list_skills_by_domain",
  "type": "esql",
  "description": "List all agent skills within a specific domain category. Returns key information for skills in domains like finance, insurance, or life_sciences, sorted by rating.",
  "tags": ["consultant agent skills"],
  "configuration": {
    "query": "FROM agent_skills | WHERE domain == ?domain | KEEP skill_id, name, domain, short_description, tags, rating | SORT rating DESC",
    "params": {
      "domain": {
        "type": "keyword",
        "description": "Domain category to filter by (e.g., 'finance', 'insurance', 'life_sciences')"
      }
    }
  }
}
