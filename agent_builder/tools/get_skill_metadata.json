{
  "id": "get_skill_metadata",
  "type": "esql",
  "description": "Retrieve lightweight metadata for a skill without the full content. Returns skill information excluding large fields like skill_markdown and full description.",
  "tags": ["consultant agent skills"],
  "configuration": {
    "query": "FROM agent_skills \n| WHERE skill_id == ?skill_id \n| KEEP skill_id, name, domain, tags, author, version, rating, created_at",
    "params": {
      "skill_id": {
        "type": "keyword",
        "description": "Unique identifier for the skill"
      }
    }
  }
}
