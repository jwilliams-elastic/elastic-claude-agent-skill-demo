{
  "id": "get_skill_by_id",
  "type": "esql",
  "description": "Retrieve a specific agent skill document by its unique identifier. Returns the complete skill document including markdown content, description, and all metadata.",
  "tags": ["agent-skills", "mcp", "retrieval"],
  "configuration": {
    "query": "FROM agent_skills | WHERE skill_id == ?skill_id",
    "params": {
      "skill_id": {
        "type": "keyword",
        "description": "Unique identifier for the skill (e.g., 'verify-expense-policy', 'adjudicate-storm-claim')"
      }
    }
  }
}
